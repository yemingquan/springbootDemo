package ^basePath$.controller.temp^tempPath$;

import javax.validation.Valid;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RestController;

import ^basePath$.contant.Route;
import ^basePath$.controller.BaseAction;
import ^basePath$.service.temp^tempPath$.^serviceName$;

import io.swagger.annotations.Api;
import io.swagger.annotations.ApiImplicitParam;
import io.swagger.annotations.ApiOperation;

@RestController
@Api(description = "^actionDescription$")
public class ^className$ extends BaseAction {

//	@Autowired
//	private ^serviceName$ ^classServiceName$;

